# Python-Network-Sniffer
Network Sniffer &amp; Logger developed using Python (Scapy/Tkinter). Implements raw socket handling, BPF filtering, HTTP payload extraction, and multithreading for non-blocking UI performance.
